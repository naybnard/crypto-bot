# SOL Price Alert Telegram Bot

Monitors Solana (SOL) price via the free CoinGecko API and sends Telegram alerts.

## Setup

```bash
git clone <your-repo-url>
cd <your-repo>
python -m venv venv
source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
